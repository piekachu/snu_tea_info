// Backend URL — Supabase Edge Function (replaces the old Apps Script web app).
// Leave empty to run in local demo mode, where created events/signups only
// persist in this browser via localStorage — useful for previewing the page,
// but not shared between visitors.
window.JOIN_APPS_SCRIPT_URL = "https://mzuxduxbzvaekuancotu.supabase.co/functions/v1/join";
